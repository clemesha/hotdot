Orbited Dissected
=================

Core components to run Orbited as a Twisted Service, 
using STOMP as the example protocol.


Install
-------

Get the Twisted Trunk:
    $ svn co svn://svn.twistedmatrix.com/svn/Twisted/trunk TwistedTrunk

Install deps:
    pip -E dot_env install orbited django

Usage
-----
In the directory 'djangoweb', type:
    
    $ django-admin.py syncdb --pythonpath='.' --settings='settings'


See Config options in `start.py`, then run:

    $ twistd -ny start.py 
